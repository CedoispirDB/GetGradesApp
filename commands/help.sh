function help() {
    printf "init -- use to initiate the application (using more than once won't work)\n"
    printf "run -- run the application manually\n"
    printf "stop -- stop application from running (to restart program use restart)\n"
    printf "restart -- restart the program in case it stops\n"
    printf "status -- get application current status"
}

help